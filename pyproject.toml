[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "vs_opc"
version = "0.1.0"
description = "Simple OPC placeholder package"
authors = [
  { name = "Your Name" }
]

[project.dependencies]

